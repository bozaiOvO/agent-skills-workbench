interface:
  display_name: "直播优化助手"
  short_description: "复盘直播逐字稿并给出可执行优化方案"
  default_prompt: "Use $livestream-optimizer to review my livestream transcript, diagnose the biggest blockers, and rewrite the opening, transitions, and closing CTA."

policy:
  allow_implicit_invocation: true
