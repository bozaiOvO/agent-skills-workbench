interface:
  display_name: "事实复审"
  short_description: "判断类任务先分事实、推断和不确定"
  default_prompt: "Use $truth-check to review this judgment for facts, inference, and uncertainty before answering."

policy:
  allow_implicit_invocation: true

